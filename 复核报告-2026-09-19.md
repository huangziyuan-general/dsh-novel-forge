# dsh-novel-forge 修复复核报告（2026-09-19）

复核对象：`47c08a5`（0.13.5 第一批）+ `b11932e`（0.13.6 第二三批），HEAD = b11932e，工作树干净。
基线：`npm test` 245/245 全绿，`npm run audit` 0 硬故障。
方式：三组深读复核（严重+高危 / 中危逻辑 / 面板+轻微），关键项逐条读代码或 `node` 直跑纯函数验证。

## 一、判定总览

| 类别 | 根因修复 | 部分修复 | 未修复 | 修出新问题/名义修复 |
|---|---|---|---|---|
| 严重 S1-S2 | 2 | — | — | — |
| 高危 H1-H5 | 4 | 1（H2） | — | — |
| 中危 M1-M17 | 11 | 4（M6/M7/M8/M15） | — | 1（M17） |
| 轻微 L1-L14 | 8 | 3（L1/L3/L4） | 3（L7/L9/L12） | — |
| 测试加固 | 方向对、真校验器 | action 覆盖约 87%，REST 面零覆盖 | | |

---

## 二、已确认修好（根因）

**S1** `lib/tools/propose-tools.js:38-43` schema 补 `title/reason/preview: string` + `missing: boolean`；`lib/proposals.js:101-103` `title` 为 null 时改缺键。宿主真校验器实跑通过，`test/smoke.test.mjs:1400` 对 `novel_propose.list` 禁止 skip。

**S2** `lib/tools/project-tools.js:255` render 链新增 `set_stage` 分支。全部 20 工具 × 所有 action 的 return 形状与 render 分支重新核对，无同类漏网。

**H1** `lib/fsio.js:206-221` server 端 `writeText(p,text,mode='auto')` 与 `createFsio` 逐分支同构（`create`→`createIfAbsent`），intent 由**宿主原子执行**（dsh-fs types.d.ts 确认），非 check-then-write 掩盖；`writeJson` 透传 mode。`proposals.js:154`、`chapter-commit.js:166`、`clone.js` 六处在 REST 通道自此真正响亮失败。

**H3** `lib/server-api.js:499/520` 补 fence；全文件 27 处路由 ↔ 27 处 `trusted(req)` 一一对应。

**H4** `lib/server-api.js:343-349` `body.workspace` 必须逐字符串命中 `collectRoots()` 否则 403；`:338` slug 过 `assertBookName`（`fsio.js:26-35` 拒空/超 64/分隔符/前导点，中文名不误杀）。

**H5** `lib/server-api.js:553` `Number.isInteger(chapterNo) && >=1`；`:571-573` 补 `rest/chapter_save` 审计（actor='user'）。

**M1** `lib/ledger.js:50-65` queryFacts 改按章号取最大（同章取后写）。实测 `[…ch12筑基, ch3练气补录]` → 正确返回筑基。

**M2（主路径）** `lib/ledger.js:29` `>=`→`=== chapter`。实测补录 ch3 → conflicts=0, added=1。

**M3** `lib/ledger.js:70-77` factsDigest 改全量 rows、cast 优先排序；供给与检测（`chapter-commit.js:126`、`content-gate.js:113`）实体集对称。

**M4** `lib/content-gate.js:22-23` 「待定/略去」移出阻断，新增 `PLACEHOLDER_SOFT_RE` 进 warnings；真占位符仍阻断（`logic.test.mjs:707` 验）。不再计入熔断。

**M5** `lib/censor.js:39-41` 「性」→ `性爱/性行为/性器官`；补 `十一~十七岁` + `11~17岁`。实测 `性格/理性/可能性` 不再命中，`12岁+抚摸`、`十七岁+恋人` 仍判 risky。

**M9（功能面）** `lib/tools/search-tools.js:82-95` cwd 兜底前加 `statSync(io.cwd).isDirectory()`，非本地后端 → degraded。远端工作区静默建本地库的洞已堵。

**M10** `lib/tools/quality-tools.js:75-79` query 用 `parseList(args.entity)` + flatMap，与 status_at 对齐。

**M11** `lib/glossary.js:23-28` removeTerm 找不到即 throw（`asset-tools.js:242` 的 `before` 已成死代码；传未 trim 的 `args.term`，带空白的已存在术语会误报不存在——从假成功变响亮报错，方向正确）。

**M12/M13/M14** `panel.js:409-435` loadContinuity/loadDiagnosis 补 openSeq 三路守卫；`:510-529` saveChapter 快照三条件比对（推演闭环，无过度守卫导致永不落地）；`:721-726` 刷新走 discardPending 两步确认。

**M16** `views/settings.js:62-63` 能力清单与活按钮口径对齐。

**L4/L5/L6/L8/L10/L11/L13/L14** 已落地（readJsonBody 4MB 上限；TTS `speaking` 旗标 + paused 挂起 + resume kick，能续播不自锁；chunkText 硬切；场景序号须带分隔符才剥；人称句界补 `!?`；提案 id 加随机后缀；character/outline 校验收紧；export 限 `<book>/` 前缀）。

**ledger 纯度**（建议 4）：`ledger.js:13-15` `now` 改必填，`new Date()` 下移到 `chapter-commit.js:98`、`quality-tools.js:98`。符合不变量 2。

**batch-draft undefined 洞**：`:222-226` 阶段二跳过并计数。**熔断解除文案**：`circuit-breaker.js:25` 改为指向真实清零通道。**账本冲突计熔断**：`chapter-commit.js:100-102` 补 recordRejection。

**产物一致性**：`lib/client.js` 为 0.13.6 最新构建，`PLUGIN_VERSION` 与 package.json 一致，新增标识逐项在产物中，无「改源码忘构建」。

**测试加固（部分达成）**：`test/smoke.test.mjs:1323` 用的是宿主真校验器 `validateJsonSchemaValue`（与 `mcp-standalone.js:175-186` 镜像宿主判据同函数）；`:1390` 实调 `output.render`；`:1322` 无 SDK 直接 fail 不静默 skip；CI 已补 setup-dev 链接；`:1399` `validated>=35` 防退化。

---

## 三、必须立即处理

### P1（修出新问题 / 名义修复）aria-label 参数名不匹配 —— M17

- `src/client/ui.js:53`：`Btn({ ..., ariaLabel })`，`:65` 据 `ariaLabel` 输出 `aria-label`，注释称「M17 修复」。
- `src/client/views/chapters.js:86,87,99`：三处调用仍写 `'aria-label': ...`（kebab）→ 解构拿不到 → `ariaLabel` 恒 `undefined` → **三个纯图标按钮的无障碍名依然被丢**。
- 失效方式与 `dataset.no` 事故同族（名字对不上不报错、静默无效果），且**无测试拦住**（`client.test.mjs:1094` 只断言直写 `h('input',{'aria-label'})` 的输入框）。
- CHANGELOG 0.13.6 已按「图标按钮对读屏器可名」发布，与实际不符。
- **修法**：调用点改 `ariaLabel:`（或 Btn 同时接受 `'aria-label'`），并补一条 Btn→DOM 的 aria-label 断言。静态自检 `audit-static.mjs` 的 dataset 面只覆盖 `data-*`，可考虑扩到白名单 props。

### P2（面板仍静默）apply 的 gate 算了但 UI 丢弃 —— M6 后半

- `lib/proposals.js:183-208` applyProposal 追加跑 contentGate（含 hiddenLeakCheck），`:63-70` 数据层已把 `leaked_hidden` 并入 blocking_issues；`server-api.js:722-723` 原样透传。
- `src/client/panel.js:318-319` 只读 `value.chapter/version`；**全 panel.js 零处引用 `.gate`** → 用户点「应用」时看不到任何泄底/门禁 warning。
- **修法**：notice 下方渲染 warning（有 blocking 时红字），或至少 `console.warn` + 提案行打标记。

### P3（新增/漏网隐患）删除确认态跨书泄漏

- `state.deleteState`（详情页两步确认状态机，`panel.js:733-737`）在 `openProject`（`:244-246`）与 `goBack`（`:613`）**都未清理**（同段落已清 discardPending/rename/listDeleteId/clone）。
- 时序：A 书点「删除」→ `confirm` → 返回列表开 B 书 → `deleteState` 仍 `confirm` → **单击 B 书「删除」直接删书**，两步确认失效。
- 同族残留：`panel.js:229` 的 `listDeleteId='busy'` 使确认行整体卸载（`views/project-list.js:186` 条件为 `=== p.name`），`:198-199` 的「删除中…」分支**永不可达**（死代码）；lorebook 的做法（`panel.js:767-771`）未搬。详情页 `deleteProject` 失败退 `confirm`（`:556`）后，busy 期间按钮文案也退回「删除」（`project-detail.js:330-331`）。
- **修法**：openProject/goBack 清 `deleteState`；busy 用行内禁用而非哨兵 id。

### P4（报告点名项未动）

- `lib/chapter-commit.js:194` 落盘后审计写失败仍会整体抛错 → `batch-draft.js:250` 把已保存章报为失败（假阴性/数据不一致）。修法：saved 审计行包 try/catch + console.warn（与 rememberSession 静默语义对齐）。
- `lib/retrieval.js:245` searchChunks 不校验章号仍存在 → 删章后、下次全量 build 前仍返回幽灵段落（M8 只做了 build 清理那一半）。
- M2 残留：`ledger.js:27-28` 冲突判定的 `latest` 仍取数组最后一条，与读侧（章号最大）口径不一 → 「补录后再改那个最大章的同章值」漏判。
- L7/L9/L12 未修：`busy` 哨兵（见 P3）；`index.js:68` 仍丢 `openForgeTab` 的 disposer、`session-watch.js:1-14` 注释与常驻 tab 现实脱节；`commitChapter` 落盘次序仍为 版本→facts→novel.json→审计。

---

## 四、部分修复的残留（可作跟进）

| 项 | 残留 | 位置 |
|---|---|---|
| H2 | 只把「无条件覆盖」收窄为「写前一刻 stat」守卫；跨源读-改-写（apply vs 润色登记）仍会丢提案索引。**修法**：版本捕获提前到 `mustReadBook` 或冲突后重读重放 | proposals.js:63/167/224/243 |
| H2 注释 | `proposals.js:164-166` 称「第二击因版本前移被拒」不成立（stat 在写前）；真正挡住连点的是 `:154` 版本文件的 `'create'`。CHANGELOG「不再可能互相覆盖」夸大 | — |
| M7 | `onDelta` 子代理路径仍被丢（报告点名项）；`:495` `retryDelayMs(attempt)` 未注入 `{rand}`；重试使瞬时故障下 token 翻倍，无测试护栏 | engine.js:442-498 |
| M8 | 增量 build 永不清理；query 出口不过滤 | retrieval.js |
| M9 | AGENTS.md 不变量 1 例外条款与实现（statSync+cwd 兜底）脱节，且新增 node:fs 用法未先改条款 | search-tools.js:82-95 |
| M3 | contextpack `clip(...,900)`/`limit=40` 仍可能截掉排 in cast 后的关键门派状态 | contextpack.js:119 |
| M15 | 批量成功后 `loadChapter` 重载**不经未保存守卫**（M14 同族反向问题）；范围判定用完成时的 batchFrom/count | panel.js:476-485 |
| L1 | standalone 守卫仍非原子（await 边界），跨进程同毫秒等长覆盖检测不了 | mcp-standalone.js:126-137 |
| L3 | `decodeWorkspaceRoots` 的 `-`→`/` 仍不可逆（含连字符的工作区照旧静默消失），只加了 warn | server-api.js:97-115 |
| L4 | 超限拿到连接重置而非 413 JSON | server-api.js:52-64 |
| L14 | `<book>/novel.json`、`facts.json`、`.novel/audit.jsonl` 仍可被 export replace 覆盖（书目内自毁未挡） | asset-tools.js:185-189 |
| 护栏 | 整工具缺席 `novel_noai_scan`/`novel_clone_project`；action 缺 `worldbook.import`、`scene.delete`、`ledger.foreshadow_payoff`、`import.import/backfill`、`polish.submit`、`project.phase(带 stage)`。约 49/56 = 87% | smoke.test.mjs:1329-1378 |
| 护栏 | **REST 面零测试**：H1/H3/H4/H5 + createServerFsio mode 全无可达用例（smoke 的 `webServer.register` 是 no-op） | — |
| 护栏 | 0.13.6 这批逻辑/竞态/无障碍/TTS 修复**均无回归用例**（`git show b11932e -- test/` 仅 6 行 now 适配） | — |

---

## 五、建议的收尾顺序

1. P1 aria-label（一行 + 一条断言）、P3 deleteState 清理（两行）—— 小改动、真实危害。
2. P2 面板显示 gate —— 让 M6 的数据层修复真正到达用户。
3. P4 两项 + M8 query 侧 —— 报告点名却未动的尾巴。
4. 测试面：REST 端点冒烟（fence/白名单/章节号/审计行/writeText mode）进 CI；护栏矩阵补 8 个缺席 action；面板竞态补 headless 用例（AGENTS.md 要求测真行为）。
5. H2 版本捕获提前 + 注释/CHANGELOG 口径修正；M9 的 AGENTS.md 条款回写（不变量优先）。

## 总体判断

第一批是干净的根因修复，且补上了「全工具 × 真校验器 × render」这类能防复发的护栏，方向完全正确。第二三批覆盖面广（多数中危与约七成轻微项确实落地，词表降级也守住了检测有效性），但呈现出统一的模式：**修到数据层就停手（M6）、修了主路径留同族反向洞（M15/P3、M2 口径）、参数名对不上导致名义修复（P1）、CHANGELOG 抢先按全量落地口径发布**。最系统性的两点是 REST 半边天仍零测试护栏、本轮修复几乎无回归用例——按项目自己的「代码强制 > 提示词自觉」，这两处不补，同类漂移还会再来一次。
