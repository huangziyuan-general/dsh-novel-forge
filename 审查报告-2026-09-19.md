# dsh-novel-forge 代码审查报告（2026-09-19）

基线：`npm test` 244/244 全绿，`npm run audit` 0 硬故障。
审查方式：四路并行深读（服务端/IO 层、工具层、纯逻辑层、客户端层），关键发现已逐条人工读代码复核。

---

## 一、严重（宿主侧必然故障，建议立即修）

### S1. `novel_propose action=list` 输出违反自身 output.schema → 必然 INVALID_TOOL_OUTPUT

- `lib/tools/propose-tools.js:38-41`：schema 对 `proposals` 数组元素声明 `additionalProperties:false`，仅 4 字段（id/chapter/status/createdAt）。
- `lib/proposals.js:96-108`：`listProposals`（0.13.1 为面板可用性加的）实际给每条塞 `title`（可为 `null`）、`missing`、`reason`、`preview`。
- 用仓库自带的宿主真校验器实测确认违规。模型每次调用 `novel_propose` 的 `list` 动作都会得到无效工具输出。
- **修法**：schema items 补 `title/reason/preview: { type: 'string' }`、`missing: { type: 'boolean' }`；`listProposals` 把 `title ?? null` 改为缺键（`...(title != null ? { title } : {})`）。

### S2. `novel_project action=set_stage` 的 render 必崩 → 工具输出被判无效

- `lib/tools/project-tools.js:158`：set_stage 返回 `{ book, action: 'set_stage', stage, next }`。
- `lib/tools/project-tools.js:255`：render 分支链只认 `init/phase/repair/check/promise`，set_stage 落进兜底分支读 `v.chapters.length` → `TypeError`。宿主行为是 render 抛错 → 输出无效（`define-tool.js` 头注释）。
- **修法**：render 链给 `set_stage` 加分支返回 `v.next`，或兜底分支按字段存在性守卫。

> S1/S2 都活在现有测试面的盲区：测试只调 `execute` 不验 render，schema 抽查只覆盖 4 个工具。建议把「全工具 × 全 action 的 schema 真校验 + render 冒烟」固化进 CI（见文末建议）。

---

## 二、高危（REST 数据面的硬约束失效 / 数据安全）

这一层是共性问题：REST 面（server-api + createServerFsio）是平行于宿主沙箱的第二实现，「同语义」的承诺多处没兑现。

### H1. `createServerFsio.writeText` 静默丢弃 `mode` 参数 → 「永不覆盖旧版」在 REST 路径失效

- `lib/fsio.js:202-205`：签名只有 `(p, text)`。
- 调用方：`lib/proposals.js:149`、`lib/chapter-commit.js:164`、`lib/clone.js` 多处都传了 `'create'`。
- 工具路径上 `'create'` → createIfAbsent（已存在响亮失败）；REST 路径变成无条件覆盖。一旦 novel.json 索引过期（并发/手工改坏），REST 会静默覆盖已有版本文件——versioning.js 头部声明的不变量在此路径不成立。
- **修法**：补上与 `createFsio` 同构的 intent 推导，`writeJson` 透传 mode。

### H2. 提案应用/登记对 novel.json 的读-改-写无版本守卫（丢失更新竞态）

- `lib/proposals.js:60/159/192/211` 全走裸 `io.writeJson`。
- 竞态窗口：面板点「应用提案」读 novel.json → 另一端「润色」完成写回 → apply 用陈旧对象整体覆盖，新登记的提案索引丢失（幽灵提案）。
- 机制现成（`writeTextIfVersion`，claim 和章节保存端点已用），只是这条链路漏接。
- **修法**：proposals 的 novel.json 写入改走版本守卫语义。

### H3. `GET /projects/:id/continuity` 与 `GET /projects/:id/diagnose` 漏 fence 头校验

- `lib/server-api.js:470-471`、`490-491`。已核实：全文件 25 处端点都有 `trusted(req)` 前置检查，唯独这两个直接进处理逻辑。
- 这两个端点会返回全书正文（diagnose 逐章读）与账本数据。文件头声明的安全模型是「fence header 防 DNS rebinding」，这两个洞把正文暴露出去。
- **修法**：补上与其他端点一致的检查。

### H4. `POST /projects` 的 `body.workspace` 基本未校验，可作为任意写根

- `lib/server-api.js:321-327`：只挡字面 `..`，任意绝对路径都能成为 fsio 根并在其下创建 `<slug>/novel.json`。设计上明明有受控的 `config.workspaceRoot`。
- ⚠️ 待人工确认宿主 `ctx.fs.resolve` 对 cwd 本身是否有白名单限制（若有则降级）。
- **修法**：`body.workspace` 只允许出现在 `decodeWorkspaceRoots()` 结果集内，或删掉以配置项为准。

### H5. REST 保存章节端点绕过全部硬约束链且不写审计

- `lib/server-api.js:518-545`：零门禁可以算「面板是用户编辑面」的豁免，但两点是实打实的问题：
  1. **audit.jsonl 没有这次正文变更的记录**——「谁改的这一版」在审计链上断档，与「审计必须是硬约束」的第一哲学冲突（对比 proposals/clone 都写了 actor='user' 审计）。
  2. `chapterNo = Number(segments[3])` 无整数校验：`chapters/abc` → 写出 `第NaN章` 文件 + novel.json 落 `"NaN"` 键。
- **修法**：至少补 `Number.isInteger` 校验与审计行；门禁是否豁免请确认设计意图。

---

## 三、中等（逻辑正确性 / 静默失效）

### 账本「现在」语义三处互不一致（叠加会让一致性供给侧与检测侧各说各话）

- **M1** `lib/ledger.js:45-54`：`queryFacts` 按数组最后一条取最新值，不按章号。补录的早章旧值会被当作现状注入写前简报——而 `factsAt` 同文件里专门做了「章号更大才覆盖」。**修法**：与 `factsAt` 对齐。
- **M2** `lib/ledger.js:26-35`：`applyFactUpdates` 的 `latest.chapter >= chapter` 把「补录更早章节」也拒了，与注释和 `factsAt` 的设计矛盾（补录只能手改 facts.json）。**修法**：改成 `=== chapter` 才拒，文案同步修正。
- **M3** `lib/briefing.js:128`：简报的账本摘要只按出场人物过滤，非人物实体（门派/地点/组织）的账本永远不进上下文；而检测侧按全量实体判——门禁会因「穿旧状态」拒稿，但模型写时无从知道新状态。**修法**：供给与检测对称。

### 门禁/审查词表

- **M4** `lib/content-gate.js:20`：`PLACEHOLDER_RE` 把「待定」「略去」设为阻断级，「后续安排待定」「细节略去不表」这类正常中文叙述会被硬拦，熔断计数 +1（三次锁章）。**修法**：降为 warning 或加语境约束。
- **M5** `lib/censor.js:38-39`：`INTIMACY_WORDS` 含单字「性」——`性格/理性/可能性` 全命中，校园文里与「高中生」±100 字共现概率极高，直接 error 级；年龄词缺「十二/十七/十八岁」及阿拉伯数字写法。**修法**：改双字词组，补全年龄词。
- **M6** `lib/proposals.js:131-179`：提案应用路径完全绕过内容门禁/隐藏人物泄底检查，连 warning 都没有——润色是模型产出，完全可能在改写中带出隐藏人物名（`hiddenLeakCheck`），应用即泄底且无提示。**修法**：applyProposal 至少以 warning 形式跑一遍 contentGate 塞进返回值。

### 引擎与检索

- **M7** `lib/engine.js:420`：`runViaSubagent` 写死 `attempts = 1`，`engine.retries` 配置在 web profile 的主路径（subagent 路径）静默失效；`onDelta` 也被忽略。**修法**：套上与 `run()` 相同的 retry 循环。
- **M8** `lib/retrieval.js:213-222`：索引清理只对本轮出现过的章节生效，已删除章节的块永久残留；`query` 也不校验章号仍存在 → 幽灵段落。**修法**：全书 build 时 `DELETE ... WHERE ch NOT IN (...)`，或 query 出口过滤。
- **M9** `lib/tools/search-tools.js:82-89`：`io.abs()` 分支在真机上恒为 false（宿主 resolve 返回不透明对象），实际总走 `join(io.cwd, ...)` 兜底——例外条款「经 io.abs() 取得」与实现不符，远端工作区后端会在本地 cwd 静默建库。**修法**：无法确认本地后端时走 degraded。

### 工具层静默失效

- **M10** `lib/tools/quality-tools.js:73-77`：`novel_ledger query` 的 entity 多值（「、」分隔）不生效（`status_at` 却生效了），按描述传多实体永远 0 命中。
- **M11** `lib/glossary.js:21-23`：`novel_glossary remove` 对不存在/带空白的术语静默返回「已删除」。与 `novel_worldbook` remove（不存在即抛）不一致。

### 面板竞态（模式已确立但没铺满：openSeq 守卫在 loadElements/loadChapterList/loadProposals 有，这三处漏）

- **M12** `src/client/panel.js:407-428`：loadContinuity/loadDiagnosis 无 openSeq 守卫——A 书体检在途时切 B 书，红字挂到 B 书详情页（文件注释自己都写了「串台比不显示更糟」）。
- **M13** `src/client/panel.js:492-502`：saveChapter 的 await 后无条件改写 `baseline/draftModified`——保存期间切章并打字，未保存编辑被静默标为已保存（假绿比报错更危险）。
- **M14** `src/client/panel.js:689`：「↻ 刷新」绕过未保存草稿守卫一键覆盖 `state.draft`（back 和切章都有两步确认，唯独它没有，还排在保存旁边）。
- **M15** `panel.js:437-474`：写章/批量起草成功后不刷新当前章正文——用户接着点保存会把新版本盖回旧稿。
- **M16** `settings.js:60-61`：能力清单说「写章/结构诊断面板做不了」，而对应按钮是活的且确实能做——自相矛盾。
- **M17** `src/client/ui.js:53-66`：Btn 白名单静默丢弃 `aria-label`（chapters.js 三处纯图标按钮），与 data-action 契约同族的「传了就被吞」。

---

## 四、轻微（择要）

| # | 位置 | 问题 |
|---|---|---|
| L1 | lib/ledger.js:11 | `now = new Date().toISOString()` 默认参——ledger 按约定须纯函数，此为纯度违规 |
| L2 | lib/server-api.js:318 | POST /projects 的 slug 未过 `assertBookName`（`.`/`..`/`.git` 可过，与工具面标准不一致） |
| L3 | lib/server-api.js:93 | 工作区目录名解码对含 `-` 的路径必然失真（`Documents/other-project` 极常见），该书静默从面板消失，无日志 |
| L4 | lib/server-api.js:52-60 | readJsonBody 无大小上限 |
| L5 | lib/mcp-standalone.js:121-131 | 版本守卫 check-then-write 非原子；同毫秒等长重写使守卫失效 |
| L6 | lib/batch-draft.js:126-137 | signal 中止后 results 留 undefined 洞，阶段二 `d.ok` 会 TypeError（当前无调用方传 signal，潜伏） |
| L7 | lib/circuit-breaker.js:25 | 熔断解除文案指向 `save_chapter`，实际只有 approve/scene save 清零 |
| L8 | lib/chapter-commit.js:99-102 | 账本冲突驳回不计入熔断计数 |
| L9 | lib/proposals.js:52 | 提案 id 毫秒时间戳可撞车 |
| L10 | lib/tools/project-tools.js:367 | `novel_character save` 空白名穿透校验（写入 `人物/无题.md`） |
| L11 | lib/tools/project-tools.js:289-303 | `novel_outline save_book/save_chapter` 不校验书目存在（可造幽灵书目录） |
| L12 | lib/tools/asset-tools.js:184-186 | `novel_export` 的 `file` 参数无书目内约束，replace 模式可覆盖其他书目文件 |
| L13 | lib/tools/search-tools.js:176-205 | annotate 循环缺 try/finally 保护 `db.close()` |
| L14 | REST 面 rename/delete/claim/章节保存均不写审计（与 H5 同根） |
| L15 | src/client/tts.js:153-168 | pause 落在取正文窗口内，synth 状态与 UI 状态可能脱节 |
| L16 | src/client/tts.js:26-33 | 无标点超长段产出超限块（Chrome TTS 老毛病在这类文本复现） |
| L17 | src/client/panel.js:229 | 删书确认 'busy' 哨兵导致确认行闪烁（lorebook 的做法是对的） |
| L18 | gate-metrics.js:48 | 场景序号剥离会吃掉以数字开头的标题（「3人小队：突围」→「人小队」） |
| L19 | content-gate.js:24-25 | 人称检测句界缺 ASCII `!?` |

---

## 五、通过项（专项核查结论）

- **node:fs 收口**：通过。全 lib 仅 4 处已声明例外，无越例使用（M9 属条款与实现不符，非新增违规）。
- **纯逻辑/io 分离**：基本通过。唯 ledger 的 Date 默认参（L1）；gate/phases 的 mutate-输入属灰色。
- **@deepseek-ai/* 位置**：通过（全部 peer + optional）。
- **SQL 注入**：无面。全参数化，FTS MATCH 经 bigram 包裹转义。
- **灾难性回溯**：无风险。所有长文本正则均为有界字符类，无嵌套量词。
- **批量并发锚定（0.13.4 修复）**：完整。runDraftBatch 单次 anchor 经 `run({parent})` 下传，失败降级单章自锚定，无残留多次解析；批量中途失败不回滚已产出章节的语义正确。
- **data-* 契约**：100% 一致（含 NaN 防御与缺参报错），0.13.0 那类静默断裂当前不存在。
- **CSS token**：全部与宿主真机主题包比对过，无幻影 token。
- **XSS**：全目录无 innerHTML 类，全部经 React children 渲染。
- **入口三步契约 / 模块表 / forge-tab 重试边界**：干净。
- **mcp-standalone containment**：realpath 级符号链接追踪，词法+真实路径双校验，扎实（仅 L5 非原子）。

---

## 六、修复优先级建议

1. **立即**：S1、S2（宿主侧必然故障，改动都是几行）+ H3（两行补 trusted）。
2. **高**：H1、H2（REST 版本语义）、H5 的 NaN 校验。
3. **中**：账本三处不一致（M1-M3）、门禁词表（M4-M6）、面板竞态四件套（M12-M15）。
4. **测试面加固**（防复发，与修 bug 同等重要）：
   - smoke 的输出契约用例遍历**每个工具的每个 action**用真校验器验证实际返回；
   - 对每个工具实际调用一次 `output.render(args, value)` 断言不抛。
   - S1/S2 这类问题就能在 CI 拦截，而不是真机上被发现。

## 总体评价

工程质量明显高于同类插件：纯逻辑/io 分层自律、注释里「为什么」密度极高且与代码互证、历史学费都有对应的护栏测试。主要风险集中在两处：**REST 半边天的约束弱于工具半边**（「代码强制」在用户点按钮的路径上退化成「提示词自觉」），以及**测试面只调 execute 不验 render/schema 的盲区**——本次两个严重问题都恰好落在这个盲区里。
